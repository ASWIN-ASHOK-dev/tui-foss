"""Akash's Hacker Codex / Chmod tool tests.

Tests live in `linux-tui/tests/test_tools.py` (the suite the CI discovery
runner executes). This module intentionally stays empty to avoid shadowing
the canonical test module under pytest collection.
"""
