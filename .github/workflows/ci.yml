name: CyberShell CI / Master Integration

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Build, Lint & Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.10", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f linux-tui/requirements.txt ]; then pip install -r linux-tui/requirements.txt; fi

      - name: Lint with Ruff
        run: |
          ruff check linux-tui/src linux-tui/tests

      - name: Run Test Suite
        working-directory: ./linux-tui
        env:
          PYTHONPATH: src
        run: |
          python -m unittest discover -s tests
